import { OrganizeView } from '@/components/organize/OrganizeView';

export const metadata = {
  title: 'Organize | Personal LMS',
  description: 'Cross-day project workspace for organizing thoughts and tasks',
};

export default function OrganizePage() {
  return <OrganizeView />;
}
